/-
IutLean.Basic

This is a placeholder module for the `iut-lean` scaffold. There is no
mathematical content here yet — see the repository README for the scope
and status of this project. Kept deliberately import-free so it can be
type-checked without first resolving the `mathlib4` dependency (no
`lake update` required to check this file).
-/

namespace IutLean

example : (1 : Nat) + 1 = 2 := by decide

end IutLean
